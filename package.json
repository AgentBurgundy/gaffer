{
  "name": "gaffer",
  "version": "0.0.1",
  "private": false,
  "description": "An OpenClaw agent pack that runs an autonomous AI software-development org: a CTO you chat with, plus specialized team agents that plan, build, cross-review, and ship PRs — 24/7, on a token budget.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "gaffer": "scripts/gaffer.mjs"
  },
  "scripts": {
    "pipeline": "node scripts/pipeline.mjs",
    "budget": "node scripts/budget.mjs",
    "check": "node --check scripts/lib/gaffer.mjs && node --check scripts/pipeline.mjs && node --check scripts/budget.mjs && node --check scripts/gaffer.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "openclaw",
    "ai-agents",
    "autonomous",
    "dev-team",
    "agent-pack",
    "cto"
  ],
  "files": [
    "agents",
    "skills",
    "scripts",
    "config",
    "docs"
  ]
}
